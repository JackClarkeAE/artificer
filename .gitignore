/target/
/artifacts/
# Nested Cargo projects (addons) build into their own target directories.
target/
.DS_Store
**/tests/snapshots/**/*.new.png
**/tests/snapshots/**/*.diff.png
**/tests/snapshots/**/*.old.png

# Sample meshes fetched for local scan-addon testing. Third-party content,
# not part of the project.
*.stl
*.zip
target-check/
